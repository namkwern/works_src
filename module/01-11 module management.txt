作:in_telfike
さまざまなモジュールを扱う(int,double,String)
１、ファイル入出力
２、キーボード入力
３、初期化
４、ソート(シェル)
５、計算(合計、平均)
６、順位付け
７、探索(二分、線形、最大値、最小値、カウント)
８、乱数発生
９、シフト
10、その他
11、時間
12、文字列操作
13、コマンドライン操作

Fileio(ファイル名)→Filein/Fileoutに分割
１、ファイル入出力モジュール
変数 = (位置)
変数 = ()←全部一行に
out(変数,位置)
in(配列)(配列,読み取り開始位置,終了)
out(配列)
ファイルの行数 = size() 

Sysio
２、キーボード・システム入出力モジュール
変数 = in(変数)(変数,表示する文字)
in(配列)(配列,入力回数)(配列,表示する文字)(配列,入力回数,表示する文字)

Initialvalue
３、初期化モジュール
number(配列,初期値)
number(配列,初期値,増減値)

Shellsort
４、配列シェルソートモジュール
up(配列)
down(配列)

Calculate
５、配列計算モジュール
合計 = total(配列)
変数 = average(配列)(配列,数字)
乗 = bekijou(基数,指数)整数のみ
対数 = taisuu(基数,数)整数のみ切り捨て

Rank
６、順位付けモジュール
順位配列 = up(配列,基礎値)
順位配列 = down(配列,基礎値)

Search
７．探索モジュール　線形、二分
配列番号 = search1(配列,値)
配列番号 = search1(配列,値,開始位置)
配列番号 = search2(配列,値)
配列番号 = max(配列)
配列番号 = min(配列)
個数 = count(配列,値)

PriRandom
８、乱数発生モジュール
変数 = num(最小値,最大値)
一意の乱数 = uniNum(最小,最大)
shuffle(配列)

Shift
９、シフトモジュール
left(配列,ずらす距離)
right(配列,ずらす距離)
配列 = del(配列,要素番号)　要素番号の場所を削除して詰める

Other
１０、その他のモジュール
(0 or 1) = not(1 or 0)
数値 = scorp(数値,下限,上限)　数値を範囲内に収めるように変更する
数値 = top(数値,上限)
数値 = bottom(数値,下限)
配列 = lineChange(配列)　縦横を交換する
配列 = resize(配列,数値[,値])　配列のサイズ変更とコピー、[値埋め]

Time(時間倍率)
11、時間モジュール
timerstart()
最後のstartからの時間経過 = timerstop()
sleep(long sle)　プログラム休止時間(ms)
現在時刻 = now()
引数との差 = now(long time)
stamp() タイムスタンプ　前回の時間との差を出力

Strop
12、文字列操作モジュール
個数 = count(カウント元,カウントする文字)
文字列 = selectin(始端文字列、終端文字列)
中間削除済み文字列 = del(手前の文字列、後ろの文字列)

Command
コマンドライン操作モジュール
execCmd(String[] cmd)　コマンドを実行

cal 全要素に特定の演算

一致するデータ数出力
一部一致するデータ出力
一部一致するデータ数出力
置換

ループするシフト
挿入
