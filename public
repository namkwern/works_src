direction = {
	{-1, -1},
	{0, -1},
	{1, -1},
	
	{-1, 0},
	//{0, 0},//変化量00はありえない
	{1, 0},
	
	{-1, 1},
	{0, 1},
	{1, 1},
}

//引数：チェックしたい支点のxy座標
void direction_check(cx, cy){
	for(int n = 0, n < direction; n++){	  //0方向をすべて確認
		int dx = cx;				//引数から頂いた現在の座標
		int dy = cy;
		while(cell_judge(dx, dy)){  //範囲内なら継続
			dx += direction[n][0];      //x方向の変化量が入る
			dy += direction[n][1];      //y方向の変化量が入る
      array[dx][dy];//***主な処理をここでする***//
		}
	}
}
